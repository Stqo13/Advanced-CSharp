namespace ImplementingStacksQueues
{
    public class Program
    {
        static void Main(string[] args)
        {
            //TODO:...
            /*
            int number = int.Parse(Console.ReadLine());
            int sumOfFives = 0;
            int sumOfNotFives = 0;
            List<int> fives = new List<int>();
            List<int> twos = new List<int>();
            List<int> threes = new List<int>();
            List<int> others = new List<int>();
            while (number > 0)
            {
                int lastNumber = number / 10;
                if (lastNumber%5==0)
                {
                    sumOfFives += lastNumber;
                    fives.Add(lastNumber);
                }
                else
                {
                    sumOfNotFives += lastNumber;
                    others.Add(lastNumber);
                }
                if (lastNumber % 2 == 0)
                {
                    twos.Add(lastNumber);
                }
                else if(lastNumber%3==0)
                {
                    threes.Add(lastNumber);
                }
            }
            if (sumOfNotFives==sumOfNotFives)
            {
                Console.WriteLine($"Yes => SumOfFives {sumOfFives} = SumOfNotFives {sumOfNotFives}");
            }
            else
            {
                Console.WriteLine($"No => SumOfFives {sumOfFives} != SumOfNotFives {sumOfNotFives}");
            }
            Console.WriteLine($"Count of fives = {fives.Count}");
            Console.WriteLine($"Count of twos = {twos.Count}");
            Console.WriteLine($"Count of threes = {threes.Count}");
            */
        }
    }
}